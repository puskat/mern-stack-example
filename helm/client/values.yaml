# Default values for MERN client.
replicaCount: 1

image:
  repository: client
  pullPolicy: Never

service:
  type: NodePort 
  port: 80        
  targetPort: 80  


livenessProbe:
  initialDelaySeconds: 30
  periodSeconds: 10

readinessProbe:
  initialDelaySeconds: 5
  periodSeconds: 10

